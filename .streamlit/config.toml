[client]
toolbarMode = "minimal"

[server]
headless = true
port = 8502
address = "127.0.0.1"
enableCORS = false
maxUploadSize = 50
# baseUrlPath=app tylko na produkcji — patrz deploy/cda-streamlit.service

[browser]
gatherUsageStats = false
